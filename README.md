# Student-Management-System
A minor project with implementation of basic Java Fundamentals with JFrame form, integration with backend using MySQL DBMS with the JDBC connector to perform basic CRUD operations on data to and from the database.
